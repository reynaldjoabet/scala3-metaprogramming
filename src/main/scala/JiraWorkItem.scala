package example
// Define an enumeration for all Jira work item types

enum JiraWorkItemType{
  case Story, Task, Bug, Epic, Subtask, Improvement, NewFeature, Incident, ServiceRequest, ChangeRequest
}